# 第 4 步收尾执行规格：code-atlas 命名统一 + install 子命令

> 你是执行 Agent。本规格书是唯一任务依据。完成后按 §5 回传。
> 工作仓：`~/development/scan_repo_map`（master，基线 225d55e）。
> 只许动该仓；wisdom_app 仓一律不碰。
> 背景：对外发布定名 **Code Atlas**；skill 命令名、目录名、bin 名、npm 包名统一为
> `code-atlas`（连字符，四位一体）。npm 包名已核实未被占用。

## 1. bin 重命名（不许留兼容层）

- `bin/scan_repo_map.mjs` → `bin/code-atlas.mjs`（git mv）。
- `package.json`：`bin: { "code-atlas": "./bin/code-atlas.mjs" }`；name 已是 `code-atlas`，version 保持 2.1.0。
- 引用同步（逐一 grep 确认无遗漏）：`test/*.mjs`、`test/golden.sh`、`SKILL.md`、`README*.md`、`docs/*.md`（docs 里的历史规格书如引用旧路径属于历史记录，不改；只改现行指引）、`.github/workflows/`（若有）。
- CLI `-v` 输出改为 `code-atlas v2.1.0`；header 的 `# Generated by:` 行中的 `scan_repo_map.mjs` 改为 `code-atlas.mjs`（属 header 有意差异行，golden 的 normalize 已剥离该行，不影响对拍）。
- 验收：`grep -rn "scan_repo_map.mjs" --include="*.mjs" --include="*.sh" --include="*.json" .` 除 docs/ 历史规格外应为零。

## 2. SKILL.md 更新

- frontmatter `name: code-atlas`（description 保持三语触发词，补一句斜杠命令说明）。
- 安装路径统一为 `~/.agents/skills/code-atlas`（及各平台对应目录下的 `code-atlas`）。
- 新增「手动配方」一节，让 agent 能把自由文本参数映射为动作：
  - `/code-atlas build [目录]`：扫描/重建地图（默认当前仓根，输出 `docs/ast-maps/` 或用户指定）；
  - `/code-atlas check`：只查 freshness（比对地图 `# Source Commit` 与 `git rev-parse HEAD`、`# Worktree`），报告过期与否，不重扫；
  - `/code-atlas merge <地图文件...>`：调用 `--merge` 合并。
  每个配方写完成标准（build：打印 ✅ Generated 且 header 五字段非空；check：明确回答"新鲜/过期 + 证据"；merge：打印 ✅ Merged）。

## 3. install 子命令（`code-atlas install`，npm 发布后的主安装路径）

在 bin/code-atlas.mjs 增加子命令分发：`code-atlas install` 时执行安装逻辑，其余参数维持现 CLI。
安装逻辑（新文件 `src/install.mjs`）：
1. 探测以下 skills 根目录，哪个存在就装进哪个（存在 = 目录已存在；都不存在则全部列出并创建用户选择的，或默认创建 `~/.agents/skills`）：
   - `~/.claude/skills`（Claude Code）
   - `~/.agents/skills`（Kimi Code / Codex / pi / Hermes 通用）
   - `~/.codex/skills`（Codex CLI）
   - `~/.config/opencode/skills`（opencode）
2. 对每个目标：把 skill 本体复制为 `<root>/code-atlas/`（内容 = SKILL.md + bin/ + src/ + packages/ + package.json + README*；排除 node_modules、test、docs、.git），然后在其中运行 `npm install --omit=dev`（失败则明确报错并继续下一个目标）。
3. 已存在目标时：覆盖前打印将要替换的路径，版本相同则跳过（比 package.json version）。
4. 全程打印每个动作与结果；最后汇总：安装到哪些目录、各自版本、跳过/失败原因。
5. 纯 Node API 实现（fs.cpSync 等），不许 shell 调用（Windows 兼容）；`~` 用 os.homedir()。
   豁免：`npm install` 需派生 npm 子进程（Windows 上 npm.cmd 需 shell:true——Node ≥18.20
   对 .cmd 直接 spawn 抛 EINVAL），属文件操作之外的有意例外，与 src/install.mjs 文件头注释对齐。
6. 幂等：重复运行结果一致。

## 4. 语言列表补 JSX

README（三语）与 `--help` 文案中语言表述补 JSX（"TSX/JSX"）。engine 已支持 `.jsx`（LANG_EXTENSIONS javascript 含 jsx），仅文案遗漏。`--help` 的 Supports 行改为 `React/TS/TSX/JSX, Python, Go, Flutter (Dart), Android (Kotlin/Java), iOS (Swift/C/C++)`。

## 5. 纪律与验收

- 每个逻辑单元独立 commit（rename / SKILL.md / install / jsx 文案），英文 conventional message。
- 全绿门槛：`npm test`（84 断言 + parity + gitignore 8）+ `bash test/golden.sh`（剥离对拍 + 快照）。
- install 子命令的测试：新增 `test/install.test.mjs`——用临时 HOME 目录（覆盖 os.homedir 或注入参数）构造 4 个假 skills 根的各种组合，断言：探测正确、复制内容完整且排除项正确、已存在跳过/覆盖逻辑、全部不存在时的行为、重复运行幂等。不得触碰真实 $HOME。
- 冷装验证：`git clone` 本仓到 /tmp，`npm install`，`node bin/code-atlas.mjs -d <wisdom_app>/lib/features/player -n t -o /tmp/m.md` 成功；`node bin/code-atlas.mjs install`（HOME 指向临时目录）成功。
- 完成后用 /code-review 自查（fixed point: 225d55e；spec = 本文件），hard violation 修掉再交付。

## 6. 回传（原文，勿转述）

1. `git log --oneline 225d55e..HEAD` + `git status`（干净）；
2. §1 的 grep 零残留证明；
3. 全量测试输出结尾 + 冷装验证两段输出原文；
4. `code-atlas install` 在假 HOME 下的一次完整运行输出；
5. /code-review 两轴报告全文及处理；
6. 冲突/自由裁量/遗留清单。
